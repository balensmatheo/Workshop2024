import { createTheme } from "@mui/material";

const theme = createTheme({
    palette: {
        type: 'light',
        primary: {
            main: '#629624',
        },
        secondary: {
            main: '#f50057',
        },
    },
});

export default theme;